<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-2xl mx-auto bg-white rounded-lg shadow-md p-8">
        <h3 class="text-2xl font-bold text-indigo-700 mb-6">Edit Your Chat: <%= chat._id %></h3>
        <p class="text-gray-600 mb-6">This chat was sent from <b class="text-indigo-600"><%= chat.from %></b> to <b class="text-indigo-600"><%= chat.to %></b></p>
        
        <form action="/chats/<%= chat._id%>?_method=PUT" method="POST">
            <textarea name="message" rows="6" class="w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 mb-4"><%= chat.message %></textarea>
            <br>
            <button class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded">
                Save Changes
            </button>
        </form>
    </div>
</body>
</html>